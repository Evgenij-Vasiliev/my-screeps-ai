/**
 * contract.architecture.js
 *
 * Машинное описание корневых архитектурных сущностей империи.
 * Источник: ARCHITECTURE_CONTRACT V3.4, раздел 2 "Иерархия управления".
 *
 * НАЗНАЧЕНИЕ:
 *   Хранить формальное описание сущностей, которые не являются подсистемами
 *   ответственности, но участвуют в Ownership Contract.
 *
 * ПРАВИЛА:
 *   - Файл является декларативным. Никакой игровой логики.
 *   - Файл не использует Memory.
 *   - Файл не выполняет проверок.
 *
 * ПОЛЯ КАЖДОЙ ЗАПИСИ:
 *   role           {string}   — архитектурная роль сущности
 *   description    {string}   — краткое назначение
 *   ownershipScope {string[]} — разделы Memory, которыми владеет сущность
 */

"use strict";

/**
 * ARCHITECTURE_CONTRACT
 *
 * Корневые архитектурные сущности империи.
 *
 * РОЛИ:
 *   ROOT_DIRECTOR — единственный источник стратегической политики.
 *                   Не управляет конкретной подсистемой.
 *                   Владеет корневым разделом Memory и делегирует
 *                   подразделы Directors-владельцам.
 */
const ARCHITECTURE_CONTRACT = {
  /**
   * EmpireDirector
   *
   * Корневой директор империи.
   * Является единственным источником стратегической политики.
   * Не является владельцем подсистемы — является владельцем корня.
   * Директора подсистем подчиняются EmpireDirector, но управляют
   * собственными разделами Memory самостоятельно.
   *
   * Источник: ARCHITECTURE_CONTRACT V3.4, раздел 2 и Закон №4.
   */
  EmpireDirector: {
    role: "ROOT_DIRECTOR",
    description:
      "Единственный источник стратегической политики империи. " +
      "Инициирует проверки состояния Directors, но не изменяет их данные напрямую. " +
      "Владеет корневым разделом Memory.empire.",
    ownershipScope: ["Memory.empire"],
  },
};

module.exports = ARCHITECTURE_CONTRACT;
