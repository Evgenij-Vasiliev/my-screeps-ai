// ===================================================
// CONSTANTS/MARKET.JS — MARKET — рынок, закупка X и приоритеты лаб
// ===================================================
// MARKET (продажа излишков и курируемый импорт), X_PURCHASE и LAB_PRIORITY.
// Выделено из constants.js; потребители берут значения через barrel
// constants.js, чтобы объекты конфига оставались одним экземпляром.
// ===================================================
// ── MARKET ───────────────────────────────────────────────────────────────
// Настройки менеджера рынка (market.manager.js). Задача 16 «Экономика»:
// раньше списки были пусты, рынок не работал, а излишки копились в терминалах
// (на shard3 — до 98k power в каждом терминале при вместимости 300k), перекрывая
// terminalNetwork и подвоз из storage. Теперь излишки продаются, но под
// жёсткими ограничителями (резервы, ценовые пороги, доля выручки, лимиты).
const MARKET = {
  // Ресурсы, которые нужно ЗАКУПАТЬ на рынке.
  // Чтобы включить/выключить закупку ресурса — просто добавь/убери строку.
  // Для X закупка НЕ безусловная: объём и разрешение считает
  // market.manager.shouldBuyX по порогам дефицита X_PURCHASE (ниже); запись
  // здесь включает сам механизм закупки.
  //
  // O, Z, H — СЫРЬЁ, КОТОРОЕ ИМПЕРИЯ НЕ МОЖЕТ ДОБЫТЬ САМА (проверено на живом
  // shard3): минералов O и Z нет ни в одной комнате, а H/L/U выработаны ниже
  // порога спавна минерал-майнера (MINERAL_MIN_AMOUNT_TO_SPAWN = 500: по
  // FIND_MINERALS в живом shard3 — H 300 в E35S39, L 415 в E36S38, U 380 в
  // E37S38; живы только K 44 835 в E35S37 и L 24 380 в E37S37).
  // При этом O и H нужны всем конечным бустам (OH = O + H — второй реагент
  // каждой верхней реакции, плюс O в первой ступени окислов), а Z — бусту MOVE
  // (XZHO2). Без импорта этих трёх ресурсов производство бустов невозможно.
  //
  // U ДОБАВЛЕН ПО ЖИВОМУ ЗАМЕРУ. U — единственное сырьё добычного буста XUHO2
  // (U → UO → UHO2 → XUHO2, harvest ×7), а его минерал в E37S38 выработан до
  // 380 единиц (≈76 реакций). Без импорта вся добычная ветка мертва физически,
  // хотя XUHO2 — единственный буст, который окупается CPU: он сокращает число
  // вызовов creep.harvest() у майнера с одного раз в 2 тика до одного раз в 10.
  // Цена U на живом рынке — 9.64 кредита за единицу (29 заявок, 567 231 в
  // продаже), то есть сырьё дешевле любой альтернативы.
  // ГОТОВЫЕ БУСТ-МИНЕРАЛЫ — ТОЖЕ ИМПОРТ (живой разбор shard3, см. ниже).
  //
  // ПОЧЕМУ ОНИ ЗДЕСЬ. Собственное производство X-бустов оказалось замороженным
  // не мощностью, а логистикой реагентов: правило LAB_KEEP (3000) вместе с
  // подсчётом запаса донора БЕЗ лабораторий (terminalNetwork.totalResource)
  // запрещает вывоз реагента из комнаты, которая сама его расходует. Живой
  // shard3: KH2O 1500 в терминале E35S39 → availableToGive = 1500 − 3000 = 0,
  // то есть единственный производитель KH2O не может отдать НИ ЕДИНИЦЫ, а
  // XKH2O-тройки четырёх остальных комнат стоят с пустыми реагентными лабами.
  // Итог замера: по империи работала РОВНО ОДНА реакция (E35S39.labs,
  // XKH2O 5 единиц за 60 тиков), буст-лабы пусты, бустов нет ни у одного крипа.
  // Рынок при этом полон (XKH2O 7395 единиц от 1532, XZHO2 8132 от 1206,
  // XUHO2 2569 от 466), а на счету 1.17 млрд кредитов — то есть бутылочное
  // горло обходится дешевле, чем простой всего контура.
  //
  // РОЛЬ ПОКУПКИ. Это ПОДСТРАХОВКА и разблокировка, а не замена производству:
  // закупка включается только когда имперский запас (Storage + Terminal +
  // лаборатории, см. shouldBuyImport) падает ниже LOW, и прекращается на HIGH.
  // Пороги кратны ROOM_RESERVE (комплект = 900/600/300 × 5 комнат):
  // LOW = один комплект, HIGH = три комплекта — запас на время, пока чинится
  // производственная цепочка. Цена покупки — лучшая заявка продажи, поэтому
  // покупается самый дешёвый ордер, а не любой.
  BUY_RESOURCES: ["X", "O", "Z", "H", "U", "XKH2O", "XZHO2", "XUHO2"],

  // Пороги закупки критичного импорта (кроме X — у него свой X_PURCHASE ниже).
  // Смысл тот же, что у X: LOW/HIGH задают гистерезис, а не «покупать всегда»:
  //   запас < LOW           → закупка разрешена, цель — довести до HIGH;
  //   LOW ≤ запас < HIGH    → закупка запрещена (тратится накопленное);
  //   запас ≥ HIGH          → закупка прекращена.
  // Запас считается по империи (Storage + Terminal + лаборатории).
  // MAX_AMOUNT — потолок одной сделки (защита от «жирного» ордера).
  //
  // ПОЧЕМУ У O БУФЕР БОЛЬШЕ ОСТАЛЬНЫХ: O — участник ВСЕХ пяти цепочек (OH —
  // второй реагент каждой верхней реакции, плюс O в первой ступени окислов),
  // суммарная потребность резерва ≈ 31 000 единиц (раздел 7 плана). Живой замер
  // после первого деплоя показал, что при LOW = 5000 запас O колеблется вокруг
  // порога, и комнаты-заводы (E37S37/E37S38, у которых своего O нет вовсе)
  // периодически стоят без сырья. Буфер 10000..30000 даёт цепочке работать без
  // микропростоев; целевое значение уточняется по фактическому расходу.
  IMPORT: {
    O: { LOW: 10000, HIGH: 30000, MAX_AMOUNT: 5000 },
    Z: { LOW: 2000, HIGH: 10000, MAX_AMOUNT: 5000 },
    H: { LOW: 5000, HIGH: 20000, MAX_AMOUNT: 5000 },
    // U: расход ~400 единиц за 1500 тиков (U → UO → UHO2 → XUHO2, 1:1 по
    // ступеням при мощности добычных троек 0.25 ед/тик). Буфер 5000..20000 —
    // запас на десятки тысяч тиков, цена на живом рынке 9.64 кредита за единицу.
    U: { LOW: 5000, HIGH: 20000, MAX_AMOUNT: 5000 },
    // Готовые бусты: комплект империи = ROOM_RESERVE × 5 комнат
    // (XKH2O 900 → 4500, XZHO2 600 → 3000, XUHO2 300 → 1500).
    // LOW — начать закупку, HIGH — три комплекта (запас на десятки тысяч
    // тиков при текущем темпе выдачи), MAX_AMOUNT — потолок одной сделки.
    XKH2O: { LOW: 4500, HIGH: 13500, MAX_AMOUNT: 3000 },
    XZHO2: { LOW: 3000, HIGH: 9000, MAX_AMOUNT: 2000 },
    XUHO2: { LOW: 1500, HIGH: 4500, MAX_AMOUNT: 1500 },
  },

  // ── АВТОЗАКУПКА РЕАГЕНТОВ ЛАБ (страховка всего LAB_PLAN) ──────────────
  // ТЗ владельца: «если чего-то не хватает для лаб — и нет во всех комнатах —
  // автоматически закупаем». Список IMPORT выше — РУЧНОЙ и курируемый (там
  // только то, что владелец признал критичным). Здесь — АВТОМАТИКА по данным
  // плана: market.manager берёт набор реагентов из lab.recipes.requiredReagents
  // (все recipeA/recipeB всех троек LAB_PLAN) и по каждому смотрит запас ВСЕЙ
  // империи (Storage + Terminal + лаборатории всех собственных комнат). Если
  // запаса меньше планового резерва (сумма LOW троек, которые этот реагент
  // тратят) — ресурс считается отсутствующим и покупается.
  //
  // Зачем: 25.09.2026 цепочка XUHO2 стояла не из-за кода, а потому что UHO2 не
  // было ВО ВСЕХ комнатах (0 единиц), и тройку E35S37.labs3 пришлось
  // разблокировать покупкой вручную. Этот блок делает то же самое сам, и не
  // только для UHO2, а для любого реагента плана: KH2O, ZHO2, UHO2, KH, OH,
  // ZO, UO, K, L — как только их не стало по империи.
  //
  // ЧЕГО БЛОК НЕ ТРОГАЕТ. Ресурсы с курируемыми порогами выше (MARKET.IMPORT
  // и X_PURCHASE) он пропускает: у них свои LOW/HIGH и свои получатели, и
  // дублировать их здесь нельзя (иначе две подсистемы купят одно и то же в
  // один тик). То есть O/Z/H/U/XKH2O/XZHO2/XUHO2 и X остаются на прежних
  // правилах, а новым охватом становятся промежуточные соединения и базовые
  // минералы плана.
  LAB_IMPORT: {
    // Выключатель всей автоматики (false — market.manager её не запускает).
    ENABLED: true,

    // Порог закупки и цель — от ПЛАНОВОГО РЕЗЕРВА (requiredReagents().low):
    //   запас империи < max(MIN_LOW, low × LOW_RATIO)  → закупка разрешена;
    //   цель — max(MIN_HIGH, low × HIGH_RATIO).
    // LOW_RATIO = 1 значит «покупаем, когда в империи меньше одного комплекта
    // плана»; HIGH_RATIO = 2 — «добираем до двух комплектов», чтобы следующий
    // дефицит не случился сразу после сделки.
    LOW_RATIO: 1,
    HIGH_RATIO: 2,

    // Полы для реагента, который нужен одной тройке (её LOW бывает 300–800):
    // без MIN_LOW покупка не срабатывала бы вовсе на малых резервах.
    MIN_LOW: 500,
    MIN_HIGH: 1500,

    // Потолок одной сделки: страховка от «жирного» ордера и от того, что одна
    // покупка выгребет терминал-получатель под завязку.
    MAX_AMOUNT: 3000,

    // Сколько ресурсов проверяем за один запуск рынка (обход книги заявок —
    // самая дорогая часть менеджера). Остальные ресурсы проверятся в следующих
    // запусках: список обходится по кругу, поэтому голодный реагент не
    // «застрянет» в конце очереди.
    MAX_RESOURCES_PER_RUN: 3,

    // АБСОЛЮТНЫЙ потолок цены (кредитов за единицу) по ресурсу. Нужен там, где
    // относительное правило MAX_BUY_PRICE_RATIO неприменимо: у промежуточных
    // соединений (UHO2, ZHO2, KH2O) рынок тонкий, лучшая продажа в разы дороже
    // лучшей покупки, и сравнение с чужим бидом отклоняло бы ЛЮБУЮ сделку —
    // ровно так уже было с готовыми бустами (см. MARKET.BUY_MAX_PRICE).
    // Пусто/нет ключа → работает относительный предохранитель.
    MAX_PRICE: {},
  },

  // Энергетический пол терминала при ЗАКУПКЕ (комиссия сделки платится
  // энергией терминала-получателя, и её резерв нельзя проедать).
  //
  // Раньше здесь использовался SELL_RESERVE.energy = 100000, и в живом shard3
  // (энергия терминалов 48–77k) не проходила НИ ОДНА закупка, включая X: импорт
  // сырья был закрыт не ценой, а полом. 10000 — тот же уровень, что у
  // приоритетных отправок терминальной сети (TERMINAL_NETWORK.
  // PRIORITY_ENERGY_FLOOR), то есть закупка сырья приравнена к приоритетной
  // логистике, а не к обычной. Пол понижен вместе с ним по тому же живому
  // замеру (энергия терминалов 6.8–26.5k): при 20000 закупать могла ровно одна
  // комната, и пополнение буст-минералов зависело от неё одной.
  BUY_ENERGY_FLOOR: 10000,

  // Ресурсы, которые нужно ПРОДАВАТЬ на рынке (излишки экономики).
  // Критерий включения: ресурс не расходуется текущими реакциями комнат
  // (Memory.rooms[*].labs*) либо его запас многократно больше расхода.
  // Порядок списка задаёт только «точку старта» — менеджер обходит его
  // по кругу (round-robin), чтобы первый ресурс не съедал весь лимит сделок.
  //
  // O, H, U, OH УБРАНЫ ИЗ ПРОДАЖИ (проверено на живом shard3): O и H — сырьё,
  // которого в империи нет вовсе (минералов O нет, H выработан), U — реагент
  // XUHO2 при почти выработанном минерале, OH — второй реагент всех верхних
  // реакций. Раньше они продавались «до резерва 1000», и в живом shard3 от O
  // осталось ровно 1005 единиц (≈ SELL_RESERVE.O) при пустых цепочках бустов:
  // рынок распродал именно то сырьё, из которого делаются бусты. K и L остаются
  // в продаже: их минералы живы (K 46k в E35S37, L 26k в E37S37 плюс 17k в
  // storage E37S37), а рынок продаёт только излишек терминала выше резерва.
  SELL_RESOURCES: ["power", "energy", "battery", "K", "L", "X"],

  // Сколько ресурса НЕ отдаём из терминала (резерв считается на КАЖДЫЙ
  // терминал отдельно). Рабочие запасы ролей (лаборатории, стройка, линки)
  // лежат в storage и продажей не затрагиваются — lab.worker берёт реагенты
  // из терминала, только если они там есть, иначе из storage.
  SELL_RESERVE: {
    power: 5000, // запас на будущий PowerSpawn/GPL
    // 100000 — НЕ TERMINAL_SUPPLY.ENERGY_MIN. Раньше комментарий утверждал
    // равенство, и это было неверно уже тогда: ENERGY_MIN понижен до 10000
    // (см. TERMINAL_SUPPLY), а запас на продажу энергии оставлен прежним, потому
    // что продавать энергию имеет смысл только при реальном избытке storage
    // (roomMaySell), а предохранитель комиссии продажи — отдельный
    // MARKET.SELL_ENERGY_FLOOR = 10000. На живом shard3 энергия терминалов
    // 6.8–26.5k, то есть через sellableFrom энергия не продаётся вообще: это
    // осознанный «мёртвый» резерв, а не рабочий порог.
    energy: 100000,
    battery: 0,
    U: 1000,
    K: 1000,
    L: 1000,
    O: 1000,
    H: 1000,
    X: 1000,
    OH: 1000,
  },

  // ЭНЕРГОПОЛ ПРОДАЖИ. Комиссия сделки платится энергией терминала-продавца, и
  // её резерв проедать нельзя. Раньше эту роль играл SELL_RESERVE.energy
  // (100000) — на живом шарде он структурно недостижим (энергия терминалов
  // 6.8–26.5k), и проверка `energy < txCost + резерв` была ЗАКОММЕНТИРОВАНА
  // целиком (коммит 5d156cd, единственное изменение market.manager.js в нём, без
  // обоснования): продажи шли при любой энергии, а warnOnce про нехватку стал
  // недостижимым. 10000 — тот же уровень, что у BUY_ENERGY_FLOOR и
  // PRIORITY_ENERGY_FLOOR: резерв восстановлен, продажи не заблокированы.
  SELL_ENERGY_FLOOR: 10000,

  // Максимум сделок (покупок + продаж суммарно) за один тик. Сделка стоит CPU,
  // а излишки копятся тиками, поэтому лимит держим низким: он же защищает от
  // «слива» склада одной ошибкой в конфиге.
  MAX_DEALS_PER_TICK: 6,

  // Максимальный объём одной сделки (0 — без ограничения): страховка от
  // единственного «жирного» ордера, который иначе выгребет терминал за тик.
  MAX_DEAL_AMOUNT_DEFAULT: 5000,
  MAX_DEAL_AMOUNT: {
    // Энергию продаём мелкими партиями: терминал потом придётся доливать
    // воркерами (по 100 за рейс), а комиссия сделки тоже уходит энергией.
    energy: 3000,
  },

  // Минимальный объём сделки: мельче не торгуем — комиссия и CPU не окупаются.
  MIN_DEAL_AMOUNT: 100,

  // Сколько заявок покупки (по убыванию цены) рассматриваем как кандидатов на
  // сделку. Комиссия зависит от расстояния, поэтому лучшая цена не всегда
  // даёт лучшую сделку — проверяем несколько ближайших по цене.
  MAX_ORDER_CANDIDATES: 3,

  // Минимальная доля валовой выручки, которая должна остаться ПОСЛЕ комиссии:
  // `amount × price − txCost × energyPrice ≥ gross × MIN_DEAL_MARGIN_RATIO`.
  // Комиссия оплачивается энергией, а на shard3 энергия дорогая (бид ~65 кр/ед):
  // сделка с покупателем в дальнем секторе отдаёт до 80 % суммы комиссией,
  // без этой проверки продажа превращается в подарок.
  MIN_DEAL_MARGIN_RATIO: 0.25,

  // Как часто (тиков) проверять рынок. `getAllOrders` — самая дорогая часть
  // менеджера, а книга заявок меняется медленно: в остальные тики рынок не
  // опрашивается вовсе (0 обращений).
  CHECK_INTERVAL: 5,

  // Минимальный порог цены при продаже: не продавать дешевле,
  // чем (лучшая цена продажи на рынке) × этот коэффициент.
  MIN_SELL_PRICE_RATIO: 0.8,

  // Максимальный порог цены при покупке: не покупать дороже,
  // чем (лучшая цена покупки на рынке) × этот коэффициент.
  MAX_BUY_PRICE_RATIO: 1.2,

  // АБСОЛЮТНЫЙ ПОТОЛОК ЦЕНЫ ЗАКУПКИ для ресурсов, где правило MAX_BUY_PRICE_RATIO
  // неприменимо. У готовых бустов рынок тонкий: лучшая продажа в разы дороже
  // лучшей покупки (живой shard3: XKH2O 1531.98 против 265.98, XZHO2 1205.71
  // против 189.22, XUHO2 466.17 против 258.62), поэтому «не дороже лучшей
  // встречной заявки × 1.2» отклоняло ЛЮБУЮ покупку бустов — подстраховка не
  // срабатывала ни разу. Здесь вместо сравнения с чужой заявкой стоит свой
  // потолок: он защищает от роста цены, но не запрещает сделку на тонком рынке.
  // Потолки взяты с запасом ~30 % к живому лучшему предложению.
  BUY_MAX_PRICE: {
    XKH2O: 2000,
    XZHO2: 1600,
    XUHO2: 600,
  },
};

// ── MARKET.X_PURCHASE (закупка X) ────────────────────────────────────────
// X в империи не добывается: он нужен как второй реагент ВСЕХ финальных
// реакций E35S37 (KH2O/KHO2/ZHO2/LHO2/UHO2 + X → X-буст, см. LAB_PLAN),
// поэтому его запас поддерживается покупкой на рынке. Отдельной торговой
// системы нет: пороги читает существующий market.manager (шаг закупки
// BUY_RESOURCES), объём и цель считает market.manager.shouldBuyX.
//
// ПОЧЕМУ LOW И HIGH, А НЕ ОДИН ПОРОГ. Один порог даёт дребезг: покупка
// разрешена ровно в тик, когда запас упал ниже него, поэтому закупка
// распадается на случайные «пакеты», а её объём зависит от остатка на момент
// проверки. Коридор задаёт гистерезис:
//   запас < LOW           → закупка разрешена, цель — довести до HIGH;
//   LOW ≤ запас < HIGH    → закупка ЗАПРЕЩЕНА (тратится накопленный запас);
//   запас ≥ HIGH          → закупка прекращается;
//   запас снова упал < LOW → закупка разрешается заново.
// Пороги проверяются по СУММЕ империи (Storage + Terminal + лаборатории всех
// комнат, см. market.manager.empireResourceTotal), а не по терминалу одной
// комнаты: X лежит и в лабораториях-реакторах, и в терминалах доноров.
//
// LOW/HIGH — стартовые значения из ТЗ (X BUY LOW = 5000, X BUY HIGH = 15000),
// а не окончательная экономическая норма: их подбирают по фактическому расходу
// финальных троек (каждая реакция тратит 5 X за тик на тройку).
const X_PURCHASE = {
  RESOURCE: "X",

  // Целевой запас империи: ниже LOW закупка разрешена, до HIGH добираем.
  // Обязательно LOW < HIGH (гистерезис против дребезга закупки).
  //
  // ПОЧЕМУ ПОДНЯТО (было 5000/15000). Резерв пяти конечных бустов для экспансии
  // — 17 000 единиц (docs/EXPEDITION_BOOST_STOCK_PLAN.md, раздел 7), и каждая
  // единица конечного буста расходует 1 единицу X (реакция 5 + 5 → 5). Старый
  // HIGH = 15000 закрывал закупку, когда в империи уже есть 15 000 X, то есть
  // на границе нужды, а LOW = 5000 в живом shard3 (X = 6993) вообще запрещал
  // закупку: производство вставало бы на X. Кроме того, защита X от продажи
  // активна, пока запас ниже HIGH, поэтому 25 000 защищают и уже купленный X.
  LOW: 10000,
  HIGH: 25000,

  // Максимальный объём ОДНОЙ сделки закупки X: держит покупку предсказуемой
  // (комиссия платится энергией терминала-получателя) и не даёт одному
  // «жирному» ордеру выгрести лимит кредитов за тик.
  MAX_AMOUNT: 5000,

  // Максимальная цена (кредитов за единицу), которую империя готова платить.
  // 0 — без абсолютного лимита: работает только относительный предохранитель
  // MARKET.MAX_BUY_PRICE_RATIO (не покупать заметно дороже лучшего встречного
  // бида). Заполнять, если нужно жёстко ограничить бюджет закупки.
  MAX_PRICE: 0,
};

// ── LAB_PRIORITY ─────────────────────────────────────────────────────────
// Приоритетная доставка промежуточных компонентов финального производства.
// terminalNetwork делает максимум ОДНУ успешную отправку за тик (return из
// run() после OK), поэтому очередь заявок решает, кто её получит. Обычная
// сортировка «у кого меньше запас» отдаёт отправку любому голодному
// потребителю — в том числе базовому минералу, излишек которого лежит без
// дела; при этом комната финальных X-бустов стоит без реагента и не может
// варить конечный продукт.
// Поэтому заявки комнаты-финишёра получают приоритет над обычным resource
// balancing, а внутри приоритета — по фактическому дефициту (см.
// terminalNetwork.prioritizeLabRequests).
const LAB_PRIORITY = {
  // Комната-финишёр определяется данными плана, а не именем: у неё есть тройка,
  // продукт которой содержит маркер (см. lab.recipes.isFinalHub). Список
  // комнат-потребителей ресурса тоже вычисляется из плана
  // (lab.recipes.consumerRooms) — отдельного перечня комнат в конфиге нет,
  // иначе он разошёлся бы с LAB_PLAN.
  PRODUCT_MARKER: "X",

  // Множитель «срочности»: острым считается реагент финишёра, запас которого
  // ниже своего LOW-порога из LAB_PLAN (реакция уже встала). Такие заявки идут
  // ПЕРВЫМИ, перед остальными компонентами финишёра.
  URGENT_MULTIPLIER: 1,
};

module.exports = {
  MARKET,
  X_PURCHASE,
  LAB_PRIORITY,
};
